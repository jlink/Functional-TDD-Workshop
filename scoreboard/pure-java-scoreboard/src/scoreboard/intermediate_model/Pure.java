package scoreboard.intermediate_model;

public class Pure {

    public static Update computeNextScoreBoard(String readLine, ScoreBoard scoreBoard) {

        return new Update(new ScoreBoard(), "");
    }
}
